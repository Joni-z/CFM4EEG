# A128-R1 fixed execution plan — 2026-09-16

User authorized one fixed optimization recipe, full execution, and manuscript revision. The latest instruction changes execution order to single-seed breadth first; seed1/2 replication is deferred and must not launch automatically.

- Architecture: original Duplex-A128, 8 bands / 16 token rows, d128, six tri-axial blocks. No evidence establishes 8 bands as superior to 16.
- R1 changes only training-time augmentation to `jitter0.05`, `channel0.2`: one transform selected uniformly per forward. Other original supervised hyperparameters remain unchanged. Runtime caps and output identities are administrative differences.
- Original A128 36 results remain the completed reference; never label them R1.
- New supervised: 12 corpora × seed0, all on AMD. Keep the four core seed0 runs; replace core seed1/2 packs with TUAB/TUEP/ADFD/TUAR and SleepEDF/Siena/CAUEEG/ISRUC seed0 packs. Stop the five queued 145 supervised seed0 workers before moving them, and all twelve queued 145 seed1/2 workers. All 36 config files remain available for later replication.
- Attribution: own-phase replaces only measured alignment, paired against R1 on TUEV and CHB-MIT, initially seed0. Reserve these two arms for the later four-GPU FT pack; do not submit duplicate standalone controls.
- Host: BIOT and CBraMod on TUEV and CHB-MIT, initially seed0. Preserve native data, optimizer, selection, and host initialization; A128 stem uses no extra augmentation, isolating the frontend. Historical native controls only if exact protocol match is confirmed. CBraMod--CHB-MIT seed0 waits on shared 145 because measured full-length runtime exceeds the AMD wall limit; its seed1/2 workers are stopped.
- Pretraining: a single A128 masked-latent 20k-step TUEG pilot; three-hour allocation segments, at most three segments, afterany continuation, no open-ended search. These three jobs are continuation segments of seed0, not three seeds. No labels enter SSL. Full backbone strict load, gentle FT 0.1× loaded LR, original fresh LR, one epoch warmup. TUEV/CHB-MIT seed0 after FINAL checkpoint, packed with the two seed0 own-phase controls. All extra FT/host/control seeds are held.
- Preserve all validation-selected best checkpoints. Report all seeds; no test-based recipe/seed selection. Original request to retain the best test seed for release is not a basis for reporting means; release the per-seed checkpoints plus a clearly documented selection criterion.
- Nonfinite gradients, OOM, malformed tensors and missing data block training. Low early validation alone does not justify terminating a design.
- AMD: max four new allocations concurrently; 4-card packs always filled at launch with different experiments, never redundant seeds just to fill a node. A finite dispatcher submits only the active seed0 packs. Shared 145 admission requires no compute process, <100 MiB memory, and zero utilization, rechecked under a shared GPU lock.
- Existing completed and running Quad experiments retain their names; no new Quad runs. Replace only the still-pending B2 Quad pretrain.
- Manuscript main method and completed tables use original A128; R1 is a named ongoing controlled recipe until verified completed results exist. Quad-specific energy theorem cannot remain in the A128 method.
- The eight interrupted new core seed1/2 runs are deferred replications, not negative model results. Retain logs, STOP markers and validation-selected checkpoints; interrupted runs do not provide completed test scores or optimizer-resume state. A later restart needs a new attempt identity with explicit provenance.
