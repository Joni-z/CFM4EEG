# A128-R1 fixed execution plan — 2026-09-16

User authorized one fixed optimization recipe, full execution, and manuscript revision.

- Architecture: original Duplex-A128, 8 bands / 16 token rows, d128, six tri-axial blocks. No evidence establishes 8 bands as superior to 16.
- R1 changes only training-time augmentation to `jitter0.05`, `channel0.2`: one transform selected uniformly per forward. Other original supervised hyperparameters remain unchanged. Runtime caps and output identities are administrative differences.
- Original A128 36 results remain the completed reference; never label them R1.
- New supervised: 12 corpora × 3 seeds. AMD owns TUEV, IIIC, TUSZ, CHB-MIT, TUAB, TUEP, ADFD. Shared 145 owns SleepEDF, TUAR, Siena, CAUEEG, ISRUC.
- Attribution: own-phase replaces only measured alignment, paired against R1 on TUEV and CHB-MIT, seeds 0/1/2.
- Host: BIOT and CBraMod on TUEV and CHB-MIT, seeds 0/1/2. Preserve native data, optimizer, selection, and host initialization; A128 stem uses no extra augmentation, isolating the frontend. Historical native controls only if exact protocol match is confirmed.
- Pretraining: a single A128 masked-latent 20k-step TUEG pilot; three-hour allocation segments, at most three segments, afterany continuation, no open-ended search. No labels enter SSL. Full backbone strict load, gentle FT 0.1× loaded LR, original fresh LR, one epoch warmup. TUEV/CHB-MIT × 3 seeds after FINAL checkpoint.
- Preserve all validation-selected best checkpoints. Report all seeds; no test-based recipe/seed selection. Original request to retain the best test seed for release is not a basis for reporting means; release the per-seed checkpoints plus a clearly documented selection criterion.
- Nonfinite gradients, OOM, malformed tensors and missing data block training. Low early validation alone does not justify terminating a design.
- AMD: max four new allocations concurrently; 4-card packs always filled at launch, 1-card TUEP jobs on mi2101x. A finite dispatcher submits remaining packs as allocations finish. Shared 145 admission requires no compute process, <100 MiB memory, and zero utilization, rechecked under a shared GPU lock.
- Existing completed and running Quad experiments retain their names; no new Quad runs. Replace only the still-pending B2 Quad pretrain.
- Manuscript main method and completed tables use original A128; R1 is a named ongoing controlled recipe until verified completed results exist. Quad-specific energy theorem cannot remain in the A128 method.
