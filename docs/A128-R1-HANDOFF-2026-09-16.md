# A128-R1 execution handoff — 2026-09-16

Latest user instruction: single-seed breadth first; defer seed1/2. No automatic replication.
Branch: codex/a128-r1-20260916.
Worktree: /work1/chenyuyou/yifanwang/Zhizhe/CFM4EEG-a128-r1-20260916

Recipe: original Duplex-A128, 8 bands / 16 rows, d128/depth6; add only jitter0.05/channel0.2, one random transform per training forward. There is no controlled evidence that 8 bands beat 16. Original A128's 36 completed results remain the reference, with separate R1 run identities.

## Current allocation

| Job | Stage | Arms |
|---|---|---|
| 421021 A128R1_core0 | Running | TUEV, IIIC, TUSZ, CHB-MIT seed0 |
| 421037 A128R1_breadth0a | Running, admitted 2026-09-16 | TUAB, TUEP, ADFD, TUAR seed0 |
| 421038 A128R1_breadth0b | Running, admitted 2026-09-16 | SleepEDF, Siena, CAUEEG, ISRUC seed0 |
| 421024 A128_host0 | Running | BIOT TUEV/CHB-MIT and CBraMod TUEV seed0; CBraMod CHB-MIT migrated |

421022/421023 core seed1/2 packs were cooperatively stopped for deferred replication. All eight best.pt and stopped.json files were verified; test_evaluated=false. These are not negative model outcomes, not completed tests, and not optimizer-resumable checkpoints. A later rerun needs an explicit new attempt identity. Receipt: results/breadth-replan-interrupted.json.

Finite scripts/dispatch_a128.py has submitted all four active packs and exited. configs/a128_execution.json contains only seed0. All extra supervised, host and attribution seed configs remain on disk, held rather than queued. Max four new AMD allocations concurrently.

Shared 145: stopped 17 waiting A128 workers (12 extra-seed workers, plus five supervised seed0 workers moved to AMD). Only CBraMod--CHB-MIT seed0 remains eligible to acquire a truly vacant GPU. Receipt: results/breadth-replan-stopped-workers.json on 145. Other users' GPUs and jobs were untouched. Shared GPU locks are retained.

CBraMod--CHB-MIT has a measured full-length MI210 estimate of about 29 hours, exceeding the 24-hour AMD limit. Its first AMD attempt was stopped without a test and retains its artifacts. The seed0 replacement on 145 has a 47-hour cap, preserving 50 epochs. Its extra seeds are held.

## Pretraining and attribution

B2 46083824 -> 46083825 -> 46083826 is a bounded afterany continuation chain for ONE seed0 20k-step latent pretraining run, not three seeds. No extra pretraining seed is scheduled.

Mac followup /Users/mr.z/PACLock-monitor/a128_followup.py (versioned copy scripts/a128_followup_mbp.py) verifies and transfers the FINAL 20k checkpoint by SHA256. It submits one full four-GPU pack: TUEV/CHB-MIT gentle FT seed0 plus TUEV/CHB-MIT own-phase seed0. The own-phase controls are reserved for this pack and must not be submitted separately. Thus the two FT arms do not waste half a node or require extra seeds just to fill GPUs. FT waits for capacity (max four new A128 allocations), and all FT seed1/2 are deferred. The daemon has an exclusive process lock and persistent submission receipt.

## Existing Quad work

No new Quad runs. Inspected existing AMD and 145 work: no newly identified numerical or interface failure. The CBraMod--IIIC host was already at epoch 46/50 and IIIC supervised at 15/20; retain these to finish. Other continuing Quad runs preserve their original identities and fixed stop policies. Do not call a model dead because one seed is lower early, or compare validation numbers directly with test means. Existing failed CBraMod--CHB-MIT remains stopped and excluded from completed-test counts.

## Paper

/Users/mr.z/CFM4EEG-paper-a128-r1. Completed main tables use original A128, R1 is separate ongoing evidence. The new R1 table counts completed seeds with a seed0-first caption; interrupted replications do not populate it. No Quad result is relabeled A128. Replication begins only after review of breadth and validation diagnostics; do not select a recipe using the best test seed.
