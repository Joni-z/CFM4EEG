# Quad16 tokenizer portability + latent pretraining admission

User authorized both lines and reuse of historical controls; no A128 new run.

## Host trials
Six new seed0 arms: CBraMod/BIOT x TUEV/CHBMIT/IIIC. Reuse the18 existing native-host seed results in results/quad-host-controls.json; compare seed0 paired and show historical3seed spread. A single new seed is not a3seed mean. Same native optimizer, loss, classifier width, checkpoint selection, batch size, loader and stopping settings. Current-host tail equality was tested directly; older results do not preserve full source/runtime hashes, so historical-control provenance is incomplete and cannot support a claim of bitwise reproducibility across runs.
BIOT-TUEV legacy processed_biot directory was absent. Restored using existing preprocessing.biot_native on CPU cores INSIDE420562, same raw EDF sources and split seed12345. All train/val/test class counts exactly match historical results. Raw EDF checksums and rebuilt manifest are retained. Original array order/hash was not archived; multiprocessing output order may differ, so do not assert bitwise dataset identity.

Adapter: exact16band,hidden128,patch50,pacpatch50 quadrature frontend. Concatenate band features, average four adjacent patch embeddings in each host200sample window, linear2048->nativewidth +LayerNorm. This is an explicitly lossy shape adapter, not an invertibility claim. CBraMod hop200;BIOT hop100. Native token counts preserved. Native encoder, positions, classifier weights preserved at initialization. No large new transformer, no16x sequence expansion. Per-channel activation checkpointing bounds peak memory at original host batch sizes.

AMD420562 initially4 slots,6configs; four arms passed shape/tail/gradient/strict roundtrip tests and entered training. BIOT-TUEV smoke correctly refused missing data; CHB-CBraMod smoke on one repeatedly used all-negative batch eventually yielded zero gradient. Updated smoke includes both binary classes and verifies nonzero initial gradients plus finite gradients through subsequent steps. This does not change training data or sampling. Corrected CHB smoke passed on145.
LongCHB-CBraMod50epoch schedule estimated15.8h training on145 versus slowerMI210; migrated to145 shared allocator (GPU7 admitted vacant), original AMD retry skips via results/migrated-chbmit_cbramod_s0.yaml.json. No duplicate production trainer. Five arms remain assignedAMD, one145.
Recovery supervisor scripts/repair_quad_host_pack.py usesGPU1 when existingIIIC-BIOT finishes, then reruns restoredBIOT-TUEV. Originalpack supervisor paused so it cannot release the node before recovery finishes; existing training children continue. Finally resumes supervisor. Initial smoke errors remain in original pack receipt; recovery/migration receipts must be read alongside it.
AMD allocation cap24nodehours.145 shared host: respect compute processes, memory and utilization with our common GPU flock; these locks do not reserve against unrelated users.

## Latent pretrain
B2job46060424,L40S,4h allocation,training cap3h,20koptimizersteps,batch32,four random electrodes and5s crops from existing200HzTUEGclean train windows. No downstream labels, validation or test signals. The manifest records exclusions for TUAB/TUEV/TUSZ/TUEP/TUAR subjects and known TUSZ-overlap sessions. Data remains onB2.
Student raw inputs are span-masked BEFORE Sinc/Hilbert/PAC, then hidden tokens are replaced at the grid. Teacher sees clean raw input, is stop-gradient and EMA-updated. Teacher targets have batch-shared positional/DC component removed and feature LayerNorm. SmoothL1 latent prediction plus sample-level variance regularizer; no waveform/PAC reconstruction target. This is a hypothesis motivated by latent-prediction SSL, not a guaranteed gain or a novel contribution beyondPACtokenization. EEG2Rep reference: https://arxiv.org/abs/2402.17772 . Monitor sample variance and downstream validation; SSL loss alone is insufficient.
Every1ksteps atomic optimizer/teacher/student checkpoint; numbered2k,5k,10k,20k snapshots; final checkpoint and code/data provenance retained. Resume preserves optimizer+RNG/schedule step, but data-loader position is not exact and must not be represented as a bitwise continuation. Fixed20k schedule; budget stop is recorded as censored.
GPU contract on145(TUAR real arrays,not an experiment) passed5steps,finitebackprop,student invariance to altered masked samples,154/154 downstream-transfer tensors and explicit rejection of a missing tensor. CUDAsteady~.122s/step,peak~3.97GiB; B2 own realTUEG smoke still precedes training. The5step contract does not demonstrate long-run noncollapse.

## Automatic downstream comparison
After the bounded final checkpoint (>=5ksteps), local watcher /Users/mr.z/PACLock-monitor/quad16-breadth/watch_quad_pretrain_ft.py verifies SHA256 through B2->local->AMD, then submits one4cardFTpack:
TUEV/CHBMIT x standard/gentle,seed0. Standard keeps original optimizer rate; gentle multiplies all loaded parameter rates by0.1 and adds1warmupepoch. Fresh head/spatial embeddings keep1e-4. All154transferable tensors must load; no missing/mismatched/excluded weights tolerated. SameQuad architecture/patch50 and scratch references. These four fits are one pretrained checkpoint's transfer evidence, not independent pretrained seeds. Validation selects models; test results cannot select the recipe or SSL snapshot.
No full-pretrain benefit is asserted until downstream evidence exists. Pretrain3h cap +oneFTpack24nodeh limits initial expansion; existing supervisedseedjobs remain intact.
